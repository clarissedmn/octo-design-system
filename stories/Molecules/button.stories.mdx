import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Button from "../../src/components/Button";
import Signature from "../../src/components/Illustration/ThereIsABetterWay";

import "../ds.style.scss";

<Meta title="🧬Molecules|Button" />

# Button

#

#

#### Buttons allow to trigger an operation.

#

---

In OCTO Design System, we provide three types of button :

- Primary
- Outline
- Link

♿️If the button is not **explicit** ( = the title alone does not make it possible to understand its destination or its function), the `aria-label` attribute or the `title` attribute should then be used to specify the link destination or button function.

**_Exemple :_**

```

  <Button aria-label="..." kind="link" href="..." label="Link" />
```

```

 <Button title="..." kind="link" href="..." label="Link" />

```

---

<Preview>
  <Story name="All Buttons">
    <div className="spacing">
      <Button label="Primary" />
      <Button kind="outline-btn" label="Outline" />
      <Button kind="link" href="https://octo.com" label="Link" />
    </div>
  </Story>
</Preview>

## Props

<Props of={Button} />

## Primary Button

<Preview>
  <Story name="Primary Button">
    <div className="spacing">
      <Button label="Primary" />
    </div>
  </Story>
</Preview>

## Outline Button

<Preview>
  <Story name="Outline Button">
    <div className="spacing">
      <Button kind="outline-btn" label="Outline" />
    </div>
  </Story>
</Preview>

## Link

<Preview>
  <Story name="Link Button">
    <div className="spacing">
      <Button kind="link" href="https://octo.com" label="Link" />
    </div>
  </Story>
</Preview>

## Icon Button

<Preview>
  <Story name="Icon Button">
    <div className="spacing">
      <Button
        label="Primary"
        iconPosition={
          "left" && <Signature width={100} height={100} color="primaryDark" />
        }
      />
      <Button
        kind="outline-btn"
        label="Outline"
        iconPosition={
          "left" && <Signature width={100} height={100} color="primaryDark" />
        }
      />
      <Button
        kind="link"
        href="https://octo.com"
        label="Link"
        iconPosition={
          "left" && <Signature width={100} height={100} color="primaryDark" />
        }
      />
    </div>
  </Story>
</Preview>

## Disabled Button

<Preview>
  <Story name="Disabled Button">
    <div className="spacing">
      <Button label="Primary" disabled />
      <Button kind="outline-btn" label="Outline" disabled />
      <Button kind="link" href="https://octo.com" label="Link" disabled />
    </div>
  </Story>
</Preview>
