import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Button from "../../../src/ui/Molecules/Button";

import Illustration from "../../../src/ui/Atoms/Illustration";

import "../../../src/ui/Atoms/index.scss";
import "./button.scss";

<Meta title="🧬Molecules|Button" />

# Button

#

#

#### Buttons allow to trigger an operation.

#

---

In OCTO Design System, we provide three types of button :

- Primary
- Outline
- Link

♿️If the button is not **explicit** ( = the title alone does not make it possible to understand its destination or its function), the `aria-label` attribute or the `title` attribute should then be used to specify the link destination or button function.

**_Exemple :_**

```

  <Button aria-label="..." kind="link" href="..." label="Link" />
```

```

 <Button title="..." kind="link" href="..." label="Link" />

```

---

<Preview>
  <Story name="All Buttons">
    <div>
      <Button className="btn" kind="primary" label="Primary" />
      <Button className="btn" kind="primary_outline" label="Outline" />
      <Button
        className="btn"
        kind="link"
        href="https://octo.com"
        label="Link"
      />
    </div>
  </Story>
</Preview>

## Props

<Props of={Button} />

## Primary Button

<Preview>
  <Story name="Primary Button">
    <div>
      <Button className="btn" kind="primary" label="Primary" />
      <Button className="btn" kind="secondary" label="Primary" />
      <Button className="btn" kind="tertiary" label="Primary" />
      <Button className="btn" kind="quaternary" label="Primary" />
    </div>
  </Story>
</Preview>

## Outline Button

<Preview>
  <Story name="Outline Button">
    <div>
      <Button className="btn" kind="primary_outline" label="Outline" />
      <Button className="btn" kind="secondary_outline" label="Outline" />
      <Button className="btn" kind="tertiary_outline" label="Outline" />
      <Button className="btn" kind="quaternary_outline" label="Outline" />
    </div>
  </Story>
</Preview>

## Link

<Preview>
  <Story name="Link Button">
    <div>
      <Button kind="link" href="https://octo.com" label="Link" />
    </div>
  </Story>
</Preview>

## Icon Button

<Preview>
  <Story name="Icon Button">
    <div>
      <Button
        className="btn"
        kind="primary"
        label="Primary"
        iconPosition={
          "left" && (
            <ThereIsABetterWay width={100} height={100} color="primaryDark" />
          )
        }
      />
      <Button
        className="btn"
        kind="primary_outline"
        label="Outline"
        iconPosition={
          "left" && (
            <ThereIsABetterWay width={100} height={100} color="primaryDark" />
          )
        }
      />
      <Button
        kind="link"
        href="https://octo.com"
        label="Link"
        iconPosition={
          "left" && (
            <ThereIsABetterWay width={100} height={100} color="primaryDark" />
          )
        }
      />
    </div>
  </Story>
</Preview>

## Disabled Button

<Preview>
  <Story name="Disabled Button">
    <div>
      <Button className="btn" kind="primary" label="Primary" disabled />
      <Button className="btn" kind="primary_outline" label="Outline" disabled />
      <Button kind="link" href="https://octo.com" label="Link" disabled />
    </div>
  </Story>
</Preview>
